import { render, screen, fireEvent } from "@testing-library/react";
import { Add } from "@material-ui/icons";
import Avatar from "./Avatar";

describe("<Avatar />", () => {
  test.todo("Passed title with icon - it should be rendered with given props");
  test.todo("Passed title with icon - it should be rendered with given props");
});
